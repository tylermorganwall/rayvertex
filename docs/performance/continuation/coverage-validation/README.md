# Coverage validation

Build and focused tests use the release library at `dbaf8b1`. The standalone edge-extrema test checks 27,018,750 scalar samples with address/undefined-behavior sanitizers. The 245-result corpus also enables macrotiles, parallel bins, visibility and prepared assets at ten workers. These are local macOS arm64 results, not cross-platform evidence.
