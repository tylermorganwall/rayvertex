# Superseded diagnostic, not final benchmark results

This one-worker SSAO job from the interrupted worker sweep exposed the regression before `d48a259`. Its after library was `lib-complete` at renderer `811143b`; the settings and raw samples identify that older run. Final worker/quality measurements were restarted in new output directories and are reported separately. The full interrupted runs remain under `/tmp/rayvertex-optimization/structural-workers` and `structural-quality`; only the directly cited diagnostic is archived here.
