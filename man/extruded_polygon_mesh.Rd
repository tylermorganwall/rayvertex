% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extruded_polygon_mesh.R
\name{extruded_polygon_mesh}
\alias{extruded_polygon_mesh}
\title{Extruded Polygon Mesh}
\usage{
extruded_polygon_mesh(
  polygon = NULL,
  plane = "xz",
  top = 1,
  bottom = 0,
  holes = NULL,
  center = FALSE,
  flip_horizontal = FALSE,
  flip_vertical = FALSE,
  data_column_top = NULL,
  data_column_bottom = NULL,
  scale_data = 1,
  position = c(0, 0, 0),
  scale = c(1, 1, 1),
  angle = c(0, 0, 0),
  pivot_point = c(0, 0, 0),
  order_rotation = c(1, 2, 3),
  material = material_list()
)
}
\arguments{
\item{polygon}{Default \code{NULL}. Coordinates accepted by \code{xy.coords()}, an
\code{sf} object containing POLYGON or MULTIPOLYGON features, or a
\code{SpatialPolygons}/\code{SpatialPolygonsDataFrame} object. Open rings are closed;
consecutive duplicate and redundant collinear vertices are removed. Rings
must be simple with nonzero area. Holes must be strictly inside their exterior
and must not touch or overlap. Z and M coordinates are ignored.}

\item{plane}{Default \code{"xz"}. Coordinate-plane orientation: \code{xz}, \code{zx}, \code{xy},
\code{yx}, \code{yz}, or \code{zy}, matched without regard to case. Preserves rayrender's
historical coordinate permutation and first-coordinate mirror.}

\item{top}{Default \code{1}. Finite extrusion height. Either height order is
supported with outward-facing triangles. Equal heights produce one flat cap.}

\item{bottom}{Default \code{0}. Finite starting height of the extrusion.}

\item{holes}{Default \code{NULL}. For direct coordinates, strictly increasing
one-based start indices of the holes in the original input, before cleanup.
\code{NULL} or \code{0} means no holes. Spatial inputs determine holes from their rings.}

\item{center}{Default \code{FALSE}. Center the combined polygon extent before
applying the plane mapping and mesh transforms.}

\item{flip_horizontal}{Default \code{FALSE}. Flip the first polygon coordinate.}

\item{flip_vertical}{Default \code{FALSE}. Flip the second polygon coordinate.}

\item{data_column_top}{Default \code{NULL}. Numeric \code{sf} column supplying each
feature's top height. A missing column warns and uses \code{top}.}

\item{data_column_bottom}{Default \code{NULL}. Numeric \code{sf} column supplying each
feature's bottom height. A missing column warns and uses \code{bottom}.}

\item{scale_data}{Default \code{1}. Scale for heights read from data columns.}

\item{position}{Default \code{c(0, 0, 0)}. Translation of the completed mesh.}

\item{scale}{Default \code{c(1, 1, 1)}. Finite, nonzero mesh scale, uniformly or
per axis. Reflected scales preserve outward triangle winding.}

\item{angle}{Default \code{c(0, 0, 0)}. Rotation angles in degrees.}

\item{pivot_point}{Default \code{c(0, 0, 0)}. Point around which to rotate.}

\item{order_rotation}{Default \code{c(1, 2, 3)}. Order to rotate the axes.}

\item{material}{Default \code{material_list()}. Mesh material.}
}
\value{
A \code{ray_mesh} containing all polygon components. Equal-height features
contribute flat surfaces, which cannot enclose participating media.
}
\description{
Build polygon caps and walls with shared geometric vertices, including
multiple holes, spatial features, and multipart polygons.
}
\examples{
outline = rbind(c(-1, -1), c(1, -1), c(1, 1), c(-1, 1))
block = extruded_polygon_mesh(outline, top = 2)
hole = outline * 0.3
frame = extruded_polygon_mesh(rbind(outline, hole), holes = 5, top = 0.5)
}
\seealso{
\code{\link[=extruded_path_mesh]{extruded_path_mesh()}}, \code{\link[=lathe_mesh]{lathe_mesh()}}
}
